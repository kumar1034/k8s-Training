apiVersion: v1
kind: Pod
metadata:
  name: multi-container
  namespace: roboshop
spec:
  containers:
  - image: nginx
    name: nginx
  - image: almalinux
    name: almalinux-sidecar
    command:  ["sleep", "200"]
