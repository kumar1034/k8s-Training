apiVersion: v1
kind: Pod
metadata:
  name: multi-container
  namespace: roboshop
spec:
  containers:
  - image: nginx
    name: nginx
    ports:
      - containerPort: 80
  - image: almalinux
    name: almalinux-sidecar
    command:  ["sleep", "200"]
