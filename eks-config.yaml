apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: kube-cluster
  region: us-east-1

managedNodeGroups:

# `instanceTypes` defaults to [`m5.large`]
- name: spot-1 #cluster node group name
  spot: true
  ssh:
    publicKeyName: sasi12
    
